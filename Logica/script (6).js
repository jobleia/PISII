// let VB, VN, Vv, TotalDeEleitores, Total 


// VB = Number (prompt("Quantos votos em branco?"))

// VN = Number (prompt("Quantos votos nulos?"))

// Vv = Number (prompt("Quantos votos validos?"))

// TotalDeEleitores = VB+VN+Vv

// let pb = (VB/TotalDeEleitores)*100

// let pn = (VN/TotalDeEleitores)*100

// let pv = (VN/TotalDeEleitores)*100

// alert("Numero de votos brancos" + pb)
// alert("Numero de votos nulos" + pn)
// alert("Numero de votos validos" + pv)
// alert("total de votos" + TotalDeEleitores)

// let  idade,meses, dias, dias_vividos

// idade = Number(prompt("Qual a sua idade?: "))
// meses = Number(prompt("e quantos meses?: "))
// dias =Number(prompt("e quantos dias?: "))

// dias_vividos = idade * 365 + meses * 30 + dias

// alert("O total de dias vividos é: " + dias_vividos)

// let salario, reajuste, total_reajuste, valor_total_reajuste


// salario = (prompt("digite o salário: "))
// salario = Number(salario)

// reajuste = (prompt("digite o reajuste em percentual (de 1 a 100): "))
// reajuste = Number(reajuste)
 

// alert(((salario) * reajuste/100))
// valor_total_reajuste= Number((salario) * reajuste/100)


// alert("Salario total: " + (salario + valor_total_reajuste))

// let idade, meses, dias, apenasDias

// idade = Number(prompt("Idade: "))

// meses = Number(prompt("Meses: "))

// dias = Number(prompt("Dias: "))

// apenasDias = idade * 365 + meses * 30 + dias

// alert("Idade em Dias: " + apenasDias.toFixed(0))

// let n1 , antecessor 

// n1 = Number(prompt("Digite seu numero "))

// antecessor = n1 - 1 

// alert("Seu numero antecessor " + antecessor)
