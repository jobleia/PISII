// let cafe, coxinha, halls, doceDeLeite, conta

// cafe = prompt("Preço do cafe")
// cafe = Number(cafe)

// coxinha = prompt ("preço da coxinha")
// coxinha = Number(coxinha)

// halls = prompt ("preço do halls")
// halls = Number (halls)

// doceDeLeite = prompt ("preço do doceDeLeite")
// doceDeLeite = Number(doceDeLeite)



// conta = cafe + coxinha + halls + doceDeLeite

// alert("total R$" + conta.toFixed(2))

// let n1, n2, n3, n4

// n1 = prompt("qual nota")
// n1 = Number(n1)

// n2 = prompt("qual nota")
// n2 = Number(n2)

// n3 = prompt("qual nota")
// n3 = Number(n3)

// n4 = prompt("qual nota")
// n4 = Number(n4)

// media = (n1 + n2 + n3 + n4) / 4

// alert(" media Total " + media.toFixed(2))

// let vitorias, empate

// vitorias = prompt ("quantos pontos")
// vitorias = Number (vitorias)

// empate = prompt ("quantos empate")
// empate = Number (empate)

// pontos = vitorias * 3 + empate

// alert("pontuação final " + pontos)

// let cafe, coxinha, halls, doceDeLeite, conta
// cafe = prompt ("preço do cafe")
// cafe = Number (cafe)

// coxinha = prompt ("preço da coxinha")
// coxinha = Number (coxinha)

// halls = prompt ("preço do halls")
// halls = Number (halls)

// doceDeLeite = prompt ("preço do doce De Leite")
// doceDeLeite = Number (doceDeLeite)

// conta = (2*cafe) + coxinha + halls + doceDeLeite

// alert("total R$" + conta.toFixed(2))

// let dano, critico

// dano = prompt ("quanto de dano?")
// dano = Number (dano)

// danoTotal = (dano * 1.5)

// alert("Dano total: "+danoTotal )

// let dano, critico
// dano = prompt ("quanto de dano")
// dano = Number (dano)

// danoTotal = (dano * 1.5 + Math.ceil(Math.random()*100)) 

// alert("Dano Total: "+danoTotal)

let n1, n2, p1, p2, media

n1 = prompt ("qual nota")
n1 = Number (n1)

n2 = prompt ("qual nota")
n2 = Number (n2)

p1 = prompt ("qual nota")
p1 = Number (p1)

p2 = prompt ("qual nota")
p2 = Number (p2)

media = (n1 * p1) + (n2 * p2) / (p1 + p2)

alert("media: " + media.toFixed())
